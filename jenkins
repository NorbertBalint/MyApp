pipeline {
    agent any
        stages {
            stage('Check Docker Version') {
                steps {
                    sh 'docker version'
                }
            }

            stage('Build APK') {
                        steps {
                            sh './gradlew clean assembleDebug'
                        }
                    }
        }
}
